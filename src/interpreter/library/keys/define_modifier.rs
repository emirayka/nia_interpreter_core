use crate::Error;
use crate::Interpreter;
use crate::Value;

use crate::library;

pub fn define_modifier(interpreter: &mut Interpreter) -> Result<(), Error> {}

#[cfg(test)]
mod tests {
    #[allow(unused_imports)]
    use super::*;

    #[allow(unused_imports)]
    use nia_basic_assertions::*;
}
